@page
@model TimeSlotter.Pages.MyBookingsModel
@{
    ViewData["Title"] = "Мої бронювання";
    ViewData["HtmlLang"] = "uk-UA";
    var uk = new System.Globalization.CultureInfo("uk-UA");
}

<div class="h-[calc(100vh-4rem)] overflow-y-auto bg-slate-950 px-4 py-8 text-slate-100 sm:px-6">
    <div class="mx-auto max-w-3xl">
        <h1 class="text-2xl font-bold text-white">Мої бронювання</h1>
        <p class="mt-2 text-sm text-slate-400">Записи, прив’язані до вашого облікового запису.</p>
        <div class="mt-6 flex flex-wrap gap-3">
            <a asp-page="/Booking"
               class="rounded-lg bg-blue-600 px-4 py-2 text-sm font-medium text-white hover:bg-blue-500">Забронювати слот</a>
            <a asp-page="/Admin"
               class="rounded-lg border border-slate-600 px-4 py-2 text-sm text-slate-300 hover:bg-slate-800">Кабінет провайдера</a>
        </div>

        @if (!Model.Bookings.Any())
        {
            <p class="mt-10 text-center text-sm text-slate-500">У вас ще немає бронювань. Перейдіть за посиланням провайдера або відкрийте сторінку бронювання, будучи авторизованими.</p>
        }
        else
        {
            <ul class="mt-8 space-y-3" role="list">
                @foreach (var b in Model.Bookings)
                {
                    var slugQ = Uri.EscapeDataString(b.ProviderSlug.TrimStart('@'));
                    var bookAgain = $"/Booking?slug={slugQ}&date={b.Start:yyyy-MM-dd}";
                    <li class="rounded-xl border border-slate-700 bg-slate-900/80 p-4 ring-1 ring-white/5">
                        <div class="flex flex-wrap items-start justify-between gap-2">
                            <div>
                                <p class="font-medium text-white">@b.ProviderName</p>
                                <p class="mt-1 font-mono text-sm text-slate-300">
                                    @b.Start.ToString("ddd d MMM yyyy, HH:mm", uk) – @b.End.ToString("HH:mm", uk)
                                </p>
                                <p class="mt-1 text-xs text-slate-500">@b.CustomerName · @b.CustomerPhone</p>
                            </div>
                            <span class="shrink-0 rounded-md border px-2 py-0.5 text-xs @(b.SlotStatus == TimeSlotter.Models.SlotStatus.BookedByClient ? "border-amber-500/40 text-amber-200" : b.SlotStatus == TimeSlotter.Models.SlotStatus.Pending ? "border-violet-500/45 text-violet-200" : "border-slate-600 text-slate-400")">
                                @MyBookingsModel.SlotStatusUk(b.SlotStatus)
                            </span>
                        </div>
                        <a href="@bookAgain" class="mt-3 inline-block text-xs font-medium text-blue-400 hover:text-blue-300">Той самий провайдер — інша дата →</a>
                    </li>
                }
            </ul>
        }
    </div>
</div>
